# VEREDICTO revisor-visual — onboarding
Fecha: 2026-09-11 00:00
Screenshot: docs/revisiones/onboarding-375.png
Usabilidad: 25/40
Craft: 11/20
Copy (si vende): N-A
Fidelidad (si hubo referencia): N-A
Veredicto: NO LISTA
Top defectos:
1. [Todas las pantallas de pregunta/reconocimiento/loading — 01,02,03,04,05,06,07,08,09,10,11] El vacío desproporcionado arriba y sobre todo abajo (bloque de contenido ocupando ~50-60% de la altura del screenshot, con ~35-40% muerto debajo) SIGUE presente sin cambios — se confirma en las 11 capturas actuales, no era una lectura exagerada de la ronda anterior. Causa en código: `flex flex-1 flex-col justify-center` en PreguntaChips.tsx/PreguntaSlider.tsx, `flex-1 items-center justify-center` en Reconocimiento.tsx, y `min-h-dvh ... items-center justify-center` en LoadingPlan.tsx centran el bloque corto dentro de todo el alto disponible → fijar un padding-top consistente y anclar el bloque hacia el tercio superior (`justify-start` + margen fijo tras el header) en vez de centrado puro por flex-1.
2. [components/onboarding/PreguntaChips.tsx + app/onboarding/page.tsx líneas 71-83] BUG verificado en código: al presionar "Atrás" y volver a una pregunta de chips ya respondida, la selección NO se restaura — `PreguntaChips` guarda `seleccionado` en estado local sin recibir el valor ya guardado en `respuestas[paso.id]`, a diferencia de `PreguntaSlider` que sí recibe `inicial={respuestas.compromiso ?? paso.inicial}` en page.tsx línea 91. El botón "Atrás" dejar de mostrar la respuesta previa rompe control y libertad (h3) y fuerza a recordar en vez de reconocer (h6) → pasar `valorInicial={respuestas[paso.id]}` a `PreguntaChips` y usarlo como estado inicial de `seleccionado`.
3. [Títulos de las 6 preguntas de chips + los 3 reconocimientos, ej. "¿Qué te trae por aquí hoy?", "No es que pienses de más", "Ese miedo no te hace 'la intensa'"] Ningún titular resalta una palabra clave con el color de acento — solo el título final de PlanListo lo hace ("sobrepensar" en dorado, vía `tituloMarked`). La regla de conversión para onboarding exige ≥1 palabra clave en acento por titular; un titular en texto plano donde nada resalta baja jerarquía → aplicar el mismo patrón `[acento]…[/acento]` que ya existe en PlanListo.tsx a las preguntas y reconocimientos (ej. destacar "ansiedad", "temes", "sobrepensar").
4. [ChipOpcion en components/onboarding/ui.tsx] Los chips (9 de las 12 pantallas) solo llevan un borde plano de 1-1.5px, sin sombra ni elevación real — contradice la promesa explícita de FICHA-ARTE.md ("profundidad 3D, sombras reales, degradados ricos, brillo suave") que sí se cumple en la carta de tarot de PlanListo pero en ninguna otra pantalla del flujo → agregar una sombra de contacto sutil y cálida a los chips (no solo el glow del emoji al seleccionar, que ya se corrigió esta ronda).
5. [Preguntas "momento" (02), "temor" (05), "hora" (08), "atribución" (09)] Solo 2 de las 7 preguntas (motivo, ayuda) tienen microcopy de ayuda bajo el título; el resto no explica para qué sirve la pregunta → agregar una línea de microcopy corta y consistente a todas las preguntas, no solo a algunas.
