# VEREDICTO revisor-visual — landing
Fecha: 2026-09-11 00:00
Screenshot: docs/revisiones/landing-375.png
Usabilidad: 32/40
Craft: 18/20
Copy (si vende): 19/20
Fidelidad (si hubo referencia): N-A
Veredicto: NO LISTA
Top defectos:
1. [Header, esquina superior izquierda, link "LUMA" — components/landing/Hero.tsx SiteHeader L52-55] Área táctil real de ~24px de alto (`flex items-center gap-2` sin min-height ni padding vertical; el contenido es un cuadrado de 24px + texto de 16px). Muy por debajo de los 44px que el resto del kit exige — incluido "Entrar", corregido esta misma ronda a `min-h-11`, pero el barrido no incluyó el link del logo/home. Fix: agregar `min-h-11 items-center` (o py-2.5) al `<a href="/">`.
2. [Toda la página — sección "Cómo se ve en la práctica" y en general] Sigue sin ningún testimonio, cifra de uso o validación externa real (defecto abierto desde rondas previas, reconocido y no tocado esta ronda a propósito). Las 3 tarjetas de "Casos" están honestamente rotuladas "(Ejemplos ilustrativos del mecanismo.)", lo cual evita engañar, pero esa aclaración vive en texto gris de 15px fácil de saltarse mientras las 3 tarjetas SÍ parecen capturas de conversaciones reales. Fix: subir peso visual del disclaimer y, en cuanto existan, incorporar 2-3 testimonios reales con nombre/rol antes de escalar tráfico pagado.
3. [Tarjeta de tarot flotante del hero y de Casos — components/app/HeroDemoLuma.tsx L43 y L96] Texto en 8.5px ("II · La Sacerdotisa") y 9.5px ("EJEMPLO"), por debajo del piso de 11px que el propio sistema define para labels/captions (regla UX #5 del CLAUDE.md). Se repite 2 veces (hero) y 3 veces más (Casos) — no es un caso aislado. Fix: subir ambos a mínimo 11px.
4. [Barra fija inferior (StickyCtaMobile), visible solo con scroll real — components/landing/ui.tsx L254-260] Tras el fix de esta ronda (`min-h-[52px]` en vez de alto fijo) el CTA pasa de "Ver plan y precios" (corta, 1 línea, ~52px) a "Descifrar mi primera conversación" (larga, ahora 2 líneas, ~57-60px) al cruzar la oferta — la altura de la barra salta de golpe en vez de mantenerse estable. No es visible en un screenshot full-page (solo aparece con scroll), verificado por código. Fix: usar también una versión corta en el estado comercial (p. ej. "Empezar gratis") para que la barra quede siempre en 1 línea.
5. [Todos los CTA + login — app/page.tsx CTA_HREF=/onboarding, SiteHeader loginHref=/entrar] Ninguna de las dos rutas existe todavía en `app/` — hoy cualquier clic cae en 404. Esperable en esta etapa de la SECUENCIA MAESTRA (onboarding es el siguiente paso) y no bloquea el gate de "landing lista", pero debe seguir anotado como pendiente crítico antes de dar tráfico real.

Verificación de los 3 defectos marcados como corregidos esta ronda (6ª ronda: 35/40 → NO LISTA):
- #2 (skip link ausente): RESUELTO. `<a href="#contenido">Saltar al contenido</a>` es el primer elemento del body (app/page.tsx L29-34), oculto con `-translate-y-16` y visible con `focus:translate-y-0`; `<main id="contenido">` existe como destino (L36). Confirmado en código.
- #3 ("Entrar" con tap target ~40px): RESUELTO para ese link puntual — ahora `min-h-11` (components/landing/Hero.tsx L59). Pero el barrido no alcanzó al link vecino "LUMA" del mismo header (ver defecto #1 de esta ronda): la inconsistencia de tap-target dentro del MISMO componente header sigue viva, solo se movió de un link a otro.
- #4 (sticky bar no cerrable): RESUELTO. Botón "Cerrar esta barra" de 44px (`size-11`) con `setDescartada(true)` persistente por sesión (components/landing/ui.tsx L261-269). El cambio de alto fijo a `min-h-[52px]` para acomodar el texto de 2 líneas es razonable en el código, pero introduce el defecto #4 de esta ronda (salto de altura al cambiar de etiqueta) — no verificable en el screenshot full-page, solo por código.

Craft — detalle: jerarquía:4 profundidad:4 identidad:4 movimiento:3 encaje:3. Sin desvíos frente a FICHA-ARTE.md (paleta vino/cacao #1a0d13 + oro #d8a441, radios 14-16, Cormorant Garamond + Hanken Grotesk visibles en el screenshot). No coincide con los ejemplos canónicos vetados (papel+tinta verde / pizarra+latón) — pasa el test anti-clon.
Copy — detalle: idea:4 especificidad:3 emoción:4 oferta:4 acción:4. Especificidad capada por la ausencia de prueba real (defecto #2). Garantía nombrada ("la Garantía de Calma de 7 Días") con plazo, cerca del CTA de compra — check binario OK. Mecanismo "descifrar" bautizado y repetido en hero/solución/CTA — Big Idea única y consistente.
Gate: Usabilidad 32/40 (< 36) → no cumple el gate doble aunque Craft (18/20) y Copy (19/20) sí cumplen sus umbrales individuales. Los 3 defectos de la ronda anterior se corrigieron de forma correcta y verificable en código, pero esta revisión (con contexto limpio) encontró 2 defectos nuevos de la misma familia (tap-target y legibilidad de texto) que el barrido anterior no cubrió, más el salto de altura introducido por el propio fix de la barra sticky — el gate de usabilidad sigue sin cerrarse.
