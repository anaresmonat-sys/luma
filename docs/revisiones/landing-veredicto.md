# VEREDICTO revisor-visual — landing
Fecha: 2026-09-11 00:00
Screenshot: docs/revisiones/landing-375.png
Usabilidad: 37/40
Craft: 19/20
Copy (si vende): 19/20
Fidelidad (si hubo referencia): N-A
Veredicto: LISTA
Top defectos:
1. [Toda la página, franja de prueba social] Sigue sin ningún testimonio, cifra de uso o validación externa real (limitación reconocida y abierta desde rondas previas, no bloqueante para este gate pero cap el eje "especificidad y prueba" del copy en 3/4). Fix: incorporar 2-3 testimonios reales con nombre antes de escalar tráfico pagado.
2. [Todos los CTA + "Entrar" — app/page.tsx CTA_HREF=/onboarding, loginHref=/entrar] Las rutas /onboarding y /entrar no existen todavía (404 real hoy). Esperable en esta etapa de la SECUENCIA MAESTRA (la landing se construye antes que el onboarding) y no bloquea el gate de esta pantalla, pero sigue pendiente antes de dar tráfico real.
3. [Barra fija inferior — components/landing/ui.tsx StickyCtaMobile L238-244] El fix de esta ronda (`layout={!reduce}` en el contenedor motion) resuelve por código el salto de altura entre "Ver plan y precios" (1 línea) y "Descifrar mi primera conversación" (2 líneas), pero no quedó verificado con una captura de scroll real (el full-page screenshot no reproduce el estado sticky de forma fiable). Fix: confirmar con una captura de scroll en vivo que la transición de alto es realmente suave y sin parpadeo del texto.
4. [Oferta — components/landing/Oferta.tsx L118-196] Los dos planes (Anual y Mensual) usan literalmente el mismo texto de CTA ("Descifrar mi primera conversación"), coherente con el modelo onboarding-first pero sin ninguna señal de qué plan quedó "elegido" al tocar cada botón. Fix (menor, opcional): un microcopy distinto bajo cada botón que confirme el plan elegido.
5. [Footer — FooterLegal, enlaces a /privacidad /terminos /cookies /reembolsos /aviso-ia] Las rutas existen (verificado: app/privacidad, app/terminos, app/cookies, app/reembolsos, app/aviso-ia todas tienen page.tsx), pero el comentario del propio código las marca como "borrador, contenido pendiente" (archivo 47). No es un defecto de esta pantalla, pero es un pendiente real antes de lanzar tráfico pagado.

Verificación de los 3 defectos de la ronda anterior (7ª ronda: 32/40 → NO LISTA):
- #1 (link "LUMA" con tap target ~24px): RESUELTO. `<a href="/">` ahora `flex min-h-11 items-center gap-2` (components/landing/Hero.tsx L52) — mismo estándar que "Entrar" (L58). Confirmado en código.
- #3 (texto 8.5px "II · La Sacerdotisa" y 9.5px "EJEMPLO" bajo el piso de 11px): RESUELTO en ambos puntos y en los 3 casos adicionales de components/app/CasosLuma.tsx (reusa TarjetaEjemplo/CartaSacerdotisa). Grep de todos los `text-[Npx]` en components/ confirma que no queda ningún tamaño por debajo de 11px en todo el kit de landing.
- #4 (barra sticky con salto de alto al cambiar de "Ver plan y precios" a la etiqueta comercial larga): mitigado con `layout={!reduce}` en el contenedor (ui.tsx L239) — la transición de alto ahora está animada por Motion en vez de ser instantánea. Verificado en código; no reproducible en un screenshot full-page estático (ver defecto #3 de esta ronda).

Craft — detalle: jerarquía:4 profundidad:4 identidad:4 movimiento:3 encaje:4. Sin desvíos frente a FICHA-ARTE.md (paleta vino/cacao #1a0d13 + oro #d8a441, radios 14-16px consistentes vía --radius-card/--radius-button, Cormorant Garamond + Hanken Grotesk). No coincide con los ejemplos canónicos vetados (papel+tinta verde / pizarra+latón) — pasa el test anti-clon. Movimiento en 3 (no 4): de las 7 baseline del sistema, esta pantalla demuestra con solidez stagger de entrada y tap <150ms, pero 4 de las 7 (conteo de número héroe, anillos/barras, modales, celebración de hito) no tienen dónde vivir en un landing de una sola página — no hay sustituto creativo que las reemplace.
Copy — detalle: idea:4 especificidad:3 emoción:4 oferta:4 acción:4. Especificidad sigue capada por la ausencia de prueba social real (defecto #1). Garantía nombrada ("la Garantía de Calma de 7 Días") con plazo, cerca del CTA de compra — check binario OK. Mecanismo "descifrar" bautizado y repetido en hero/solución/casos/CTA final — Big Idea única y consistente.
Gate: Usabilidad 37/40 (≥36) + Craft 19/20 (≥16) → cumple el gate doble. Copy 19/20 (≥16, ningún eje ≤2) → cumple su umbral. Contraste verificado en tokens.css (texto terciario ~7:1 sobre fondo base; recap/PS del bloque invertido ~5.5:1 calculado) — sin violaciones AA detectadas. Los 3 defectos puntuales de la ronda anterior (tap target, piso tipográfico, salto de la barra sticky) están corregidos y verificados en código. Los 2 pendientes que quedan (testimonios reales, rutas de onboarding/login) son limitaciones de etapa explícitamente aceptadas por el usuario y no heurísticas de esta pantalla — quedan anotadas, no bloquean.
