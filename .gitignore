# ── Node / Next ──────────────────────────────────────────────
/node_modules
node_modules/
/.pnp
.pnp.*
/.next/
/out/
/build
/coverage
*.tsbuildinfo
.tsbuildinfo-hook
next-env.d.ts

# ── Entorno (NUNCA commitear secretos) ───────────────────────
.env
.env.*
!.env.example

# ── Deploy ───────────────────────────────────────────────────
.vercel

# ── Logs ─────────────────────────────────────────────────────
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.DS_Store
*.pem

# ── Scratch de herramientas de preview (SOLO en la raíz — docs/revisiones/ SÍ se versiona) ──
/.playwright-mcp/
/luma-*.png
/tour*.png
/abc-*.png
/landing-*.png
/l-*.png
/l[0-9]-*.png
/z*.png
/*-vp.png
/*-full.png
